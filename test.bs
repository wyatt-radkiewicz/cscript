//let arr = [0.0, 1, 2, 3, 4];
//let global: i32 = -(1 + 69 as f32);

struct TestInner {
    x: f32,
    y: f32,
}

struct TestStruct {
    a: i8,
    b: [i32; 3],
    c: TestInner,
    d: u64,
}

let test = TestStruct{
    d: 64,
    c: TestInner{
        x: 13.0,
        y: 37.0,
    },
    a: 69,
    b: [ 420, 421, 422 ],
};

extern fn print_i32(val: i32);

//extern fn print_int(val: i32) -> u0;
//extern fn input_int() -> i32;
//
//let TEST: i32 = "";
//
//fn cpy<T>(from: &[T], to: &[T]) {
//    let len = min(lenof(from), lenof(to));
//    for let i = 0..len { to[i] = from[i]; }
//}
//
//static fn printn(n: i32) -> u0 {
//	let i = new i32{5};
//	while i < n {
//		print_int(i);
//		i = i + 1;
//	}
//}
//
//fn main() -> i32 {
//	let arr: [i32] = [0, 1, 2, 3, 4];
//	arr[2] = input_int();
//	printn(arr[2]);
//	return 0;
//}

// [0, 0, 0, 0, 0]
// [i32; 5]
// 0
// i32{0}
// struct_s{}
// struct_s{
//     x: 5,
//     y: 1,
// }

